!!! 5
html.no-js(lang="en")
	head
		!=partial("includes/head")
	body
		header
			h1.logo.ch-g1-3
				a(href="/") Chico UI
				span.version= "v" + latest.version

			!=partial("includes/main-nav")

		section.main
			- if (breadcrumb.length > 0)
				!=partial("includes/breadcrumb")

			article
				!= body

		!=partial("includes/footer")